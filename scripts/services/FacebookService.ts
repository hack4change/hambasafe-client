app.service('FacebookService',
  function ($http, config, localStorageService, $q) {
  var base = config.baseServiceURL + "/v1/";
  var profileKey = "profileKey";

  return {
    login: function () {

    }
  };
}
);
